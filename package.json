{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint --ignore-pattern '**/dist/**' .",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "oxlint": "^0.16.6",
    "tsdown": "^0.12.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
