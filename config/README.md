# Runtime configuration

This directory is shipped with the repository and included in the container build (`Dockerfile` copies `./config`).

## Reference deployments

Reproducible reference deployment profiles live under:

- `config/deployments/` — single-host and split-role templates for docker compose and Helm

